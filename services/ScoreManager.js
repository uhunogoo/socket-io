export class ScoreManager {
  constructor( gameInstance ) {
    this.gameInstance = gameInstance;
  }

  calculateScore(answerTime, isCorrect, streak) {
  }
 
  async saveAnswer(playerId, answerData) {
  }
 
  async finalizeResults() {
  }
}